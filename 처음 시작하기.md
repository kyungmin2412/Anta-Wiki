# 처음 시작하기

터미널을 써본 적 없어도 괜찮습니다. 순서대로만 하면 됩니다.
**전체 15분 정도 걸리고, 이 과정은 처음 한 번만 하면 됩니다.**

---

## 1단계 — Node.js 설치하기 (한 번만)

이 위키를 돌리는 데 필요한 프로그램입니다. 무료입니다.

1. **https://nodejs.org** 에 접속합니다
2. 초록색 큰 버튼 중 **`LTS`** 라고 적힌 것을 누릅니다 (Node 22 이상이면 됩니다)
3. 내려받아진 파일을 두 번 눌러 실행합니다
4. 설치 창이 뜨면 **계속 → 계속 → 동의 → 설치**를 누릅니다. 비밀번호를 물어보면 컴퓨터 로그인 비밀번호를 넣습니다
5. "설치가 완료되었습니다"가 뜨면 닫습니다

> 이미 설치되어 있어도 다시 설치해서 문제되지 않습니다. 확신이 없으면 그냥 하세요.

---

## 2단계 — 위키 파일 내려받기

아래 주소를 브라우저 주소창에 붙여넣고 엔터를 누르면 zip 파일이 내려받아집니다.

```
https://github.com/kyungmin2412/Anta-Wiki/archive/refs/heads/claude/wiki-report-analysis-nt72af.zip
```

내려받아진 zip 파일을 두 번 눌러 압축을 풉니다.
`Anta-Wiki-claude-wiki-report-analysis-nt72af` 라는 긴 이름의 폴더가 생깁니다.

**이 폴더를 바탕화면으로 옮기고, 이름을 `안타위키` 처럼 짧게 바꾸세요.** (선택이지만 편합니다)

---

## 3단계 — 켜기

폴더를 열면 이런 파일들이 보입니다.

### 🍎 맥을 쓴다면

**`위키 실행하기.command`** 를 **두 번** 누릅니다.

> **"확인되지 않은 개발자" 경고가 뜨면** — 인터넷에서 받은 파일이라 맥이 조심하는 것입니다.
> 그 파일을 **마우스 오른쪽 클릭 → 열기 → (한 번 더) 열기** 를 누르면 실행됩니다.
> 한 번만 이렇게 하면 다음부터는 두 번 클릭으로 열립니다.

### 🪟 윈도우를 쓴다면

**`위키 실행하기.bat`** 를 **두 번** 누릅니다.

> **"Windows의 PC 보호" 파란 창이 뜨면** — **추가 정보 → 실행** 을 누르면 됩니다.

---

## 4단계 — 기다리기

검은 창이 열리고 글자가 빠르게 올라갑니다. **정상입니다.**

- **처음 실행할 때만** 준비 작업으로 2~3분 걸립니다
- 다음부터는 10초면 켜집니다

### 노란 글씨가 나와도 괜찮습니다

준비 작업 중에 이런 줄이 여러 개 지나갑니다.

```
npm warn deprecated eslint@9.39.5: This version is no longer supported.
```

**`warn`은 경고지 오류가 아닙니다.** 위키 동작과 무관한 개발용 도구에 대한 안내라
무시하면 됩니다. (지금은 Next.js가 최신 버전을 아직 지원하지 않아 그대로 두고 있습니다.)

멈춰야 하는 건 **빨간 `ERR!`** 뿐입니다.

### 다 됐다는 신호

검은 창에 이런 줄이 뜨면 성공입니다.

```
✓ Ready in 2.1s
- Local:   http://localhost:3000
```

잠시 뒤 **브라우저가 저절로 열리면서 위키가 나타납니다.**

안 열리면 브라우저 주소창에 직접 넣으세요:

```
http://localhost:3000
```

---

## 5단계 — 화면 구경하기 (선택)

리포트를 아직 안 넣었으면 빈 화면이 나옵니다.
어떻게 생겼는지 먼저 보고 싶으면, 검은 창을 닫은 뒤

- 맥: **`데모 데이터 넣기.command`**
- 윈도우: **`데모 데이터 넣기.bat`**

을 두 번 누르고, 다시 **위키 실행하기**를 누르세요.
삼성전자 예시 데이터가 들어간 화면을 볼 수 있습니다.

> 이건 제가 만든 **가짜 데이터**입니다. 실제 리포트가 아닙니다.
> 진짜 리포트를 넣기 전에 한 번 더 눌러 지우면 됩니다.

---

## 6단계 — 진짜 리포트 넣기

웹사이트의 업로드 버튼은 **API 키(유료)** 가 있어야 동작합니다.
**Claude나 ChatGPT 구독만 있으면 추가 결제 없이** 넣는 방법이 따로 있습니다. 둘 중 이미
구독 중인 쪽을 쓰면 됩니다 — 결과 품질은 어느 쪽을 써도 같습니다.

### 방법 ①  추가 결제 없음 (권장)

1. 폴더 안의 **`reports`** 폴더에 증권사 리포트 PDF들을 넣습니다
2. 이미 구독 중인 쪽을 두 번 클릭:

   | 쓰는 서비스 | 맥 | 윈도우 |
   |---|---|---|
   | Claude | `리포트 넣기.command` | `리포트 넣기.bat` |
   | ChatGPT | `리포트 넣기 (Codex).command` | `리포트 넣기 (Codex).bat` |

3. 처음이면 필요한 프로그램을 자동으로 설치하고, 로그인 창이 열립니다.
   **평소 쓰는 계정으로 로그인**하세요
4. 나머지는 알아서 합니다. 리포트를 읽고, 위키에 넣고, 종합 분석까지 만듭니다

> `reports` 폴더가 없으면 스크립트가 만들어 주고 열어줍니다.
> PDF를 넣은 뒤 다시 더블클릭하면 됩니다.
>
> 두 방법 모두 대화가 쌓이는 방식이라, 리포트를 아주 많이(수십 건 이상) 정기적으로
> 넣는다면 아래 방법 ②가 오히려 저렴합니다.

### 방법 ②  API 키 (종량제, 리포트당 약 $0.1~0.4)

웹사이트의 드래그 업로드를 쓰고 싶을 때만 필요합니다.

1. 키를 발급합니다 (구독과는 별도로 크레딧 충전이 필요합니다)
   - Claude: `console.anthropic.com`
   - ChatGPT: `platform.openai.com`
2. 위키 폴더의 **`.env.local.example`** 파일을 복사해 **`.env.local`** 로 이름을 바꾸고,
   발급받은 키에 맞는 줄의 `#`을 지우고 키 값을 채웁니다

   ```
   ANTHROPIC_API_KEY=sk-ant-여기에키
   ```

   또는

   ```
   OPENAI_API_KEY=sk-proj-여기에키
   AI_PROVIDER=openai
   ```

3. 검은 창을 닫고 **위키 실행하기**를 다시 누릅니다

---

## 알아두면 좋은 것

### 끄는 법
검은 창을 닫으면 위키도 꺼집니다. 데이터는 그대로 남아 있습니다.

### 다시 켜는 법
**위키 실행하기**를 다시 두 번 누르면 됩니다. 그게 전부입니다.

### 검은 창을 닫으면 안 되는 이유
그 창이 위키를 돌리고 있는 엔진입니다. 창을 닫으면 엔진이 멈춰서
`http://localhost:3000` 이 열리지 않습니다. 위키를 보는 동안은 **최소화만** 하세요.

### 내 데이터는 어디에 있나
폴더 안 `data` 폴더에 전부 들어 있습니다. 인터넷 어디에도 올라가지 않습니다.
백업하고 싶으면 이 `data` 폴더만 복사해 두면 됩니다.

### `localhost` 가 뭔가
"내 컴퓨터"라는 뜻입니다. 인터넷 주소가 아니라서, **내 컴퓨터에서 위키가 켜져 있을 때만** 열립니다.
다른 사람에게 이 주소를 보내줘도 열리지 않습니다.

---

## 잘 안 될 때

| 증상 | 해결 |
|---|---|
| 검은 창이 깜빡하고 바로 사라짐 | Node.js가 설치 안 된 것입니다. 1단계를 다시 하고, **컴퓨터를 껐다 켠 뒤** 다시 시도하세요 |
| "Node.js 가 설치되어 있지 않습니다" | 1단계를 하고 **컴퓨터를 재시작**하세요. 설치 직후에는 인식이 안 될 수 있습니다 |
| 브라우저에 "연결할 수 없음" | 검은 창이 아직 켜져 있는지 확인하세요. 닫혔다면 다시 실행하세요 |
| 노란 `warn` 글씨가 잔뜩 | 정상입니다. 무시하고 기다리세요 |
| 빨간 `ERR!` 글씨가 나옴 | 그 화면을 캡처해서 저에게 보여주세요 |
| 3000번 포트가 사용 중이라고 나옴 | 위키가 이미 켜져 있는 것입니다. 브라우저에서 `http://localhost:3000` 을 열어보세요 |
| 준비 작업이 실패함 | 폴더에 생긴 `설치기록.log` 파일을 보내주세요. 원인이 그 안에 적혀 있습니다 |

막히면 **검은 창 화면을 그대로 캡처해서** 보여주세요. 어디서 멈췄는지 보면 바로 알 수 있습니다.
